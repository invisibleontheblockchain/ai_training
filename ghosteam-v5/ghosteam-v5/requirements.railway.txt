# Railway-optimized requirements - Essential packages only
# Core API framework
fastapi>=0.100.0
uvicorn>=0.23.0
python-multipart>=0.0.6

# Data models and validation
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Database and caching
psycopg2-binary>=2.9.0
redis>=4.5.0
aioredis>=2.0.0

# Essential ML packages (lightweight versions)
scikit-learn>=1.3.0
pandas>=2.0.0
numpy>=1.24.0
xgboost>=1.7.0

# MLflow for model tracking (essential)
mlflow>=2.8.0

# Feast feature store (ESSENTIAL - required by application)
# Temporarily commented out due to Railway build issues
# feast[redis,postgres]>=0.34.0

# Additional feast dependencies (keeping for when feast is re-enabled)
pyarrow>=12.0.0
protobuf>=4.21.0

# Environment and configuration
python-dotenv>=1.0.0
click>=8.1.0

# Security
passlib[bcrypt]>=1.7.4
python-jose[cryptography]>=3.3.0
cryptography>=41.0.0

# Async support
asyncio-mqtt>=0.13.0
asyncpg>=0.28.0

# Monitoring (lightweight)
prometheus-client>=0.17.0

# Utilities
pyyaml>=6.0
aiofiles>=23.0.0

# Logging
structlog>=23.1.0

# HTTP client
httpx>=0.24.0

# Development (minimal)
pytest>=7.4.0
pytest-asyncio>=0.21.0